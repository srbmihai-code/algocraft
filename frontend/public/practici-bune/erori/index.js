function divide(a, b) {
  if (b === 0) {
    throw new Error("Eroare: împărțirea la zero nu este permisă");
  }
  return a / b;
}

function calculeazaMedie(numere) {
  // aici trebuie să folosești funcția divide
  // de exemplu: divide(suma, numarElemente)
}
