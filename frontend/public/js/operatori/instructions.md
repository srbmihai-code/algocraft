# Operatorii matematici și logici în JavaScript

În **JavaScript**, putem folosi operatori pentru a face calcule sau pentru a compara valori. Operatorii sunt simboluri care efectuează o acțiune pe date.

### Operatorii matematici

Aceștia se folosesc cu **numere**:

| Operator | Ce face            |
| -------- | ------------------ |
| `+`      | Adunare            |
| `-`      | Scădere            |
| `*`      | Înmulțire          |
| `/`      | Împărțire          |
| `%`      | Restul împărțirii  |
| `**`     | Ridicare la putere |

**Exemple**:

(Comentariile se notează cu `//` în JavaScript)

```javascript
let a = 10;
let b = 3;

let suma = a + b;      // 13
let diferenta = a - b; // 7
let produs = a * b;    // 30
let cat = a / b;       // 3.333...
let rest = a % b;      // 1
let putere = a ** 2;   // 100
```

### 🎯 Sarcină

Stocheaza intr-o variabila noua `x` media aritmetica a numerelor a si b (suma lor impartita la doi)