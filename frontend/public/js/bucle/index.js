// Variabilă pentru sumă
let sum = 0;

// Folosește bucla asta for pentru a aduna numerele pare de la 1 la 1000
for (let i = 1; i <= 1000; i++) {
  // TODO: Verifică dacă i este par și adaugă la sum
}
