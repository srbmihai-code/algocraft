# Bazele CSS - Setarea culorii de fundal
CSS (Cascading Style Sheets) este limbajul folosit pentru a stiliza paginile web. Cu CSS decizi cum arată elementele: culori, spațiere, aliniere, poziție, fonturi etc.

**Despre clase și selectorul cu punct**
- O *clasă* este un atribut HTML care este adaugat elementelor pe care vrem sa le grupam si este setata ca: `class="container"`.
- În CSS, pentru a da **proprietăți** elementelor din HTML trebuie să le selectăm.
- Cel mai folosit este selectorul de **clasă** în CSS, care începe cu punct: `.container { ... }`. Acesta aplică stilul tuturor elementelor cu `class="container"`. În interiorul `{ }` setăm toate atributele pentru acele elemente, după sintaxa: `proprietate: valoare;`.
- Un tag important foarte des folosit este tagul `<div>`, care nu reprezintă un conținut propriu-zis, ci o cutie în care putem pune cod pe care vrem să îl grupăm. Este foarte folosit astfel în CSS, deoarece vrem să creăm des astfel de grupuri.


### 🎯 Sarcină
Adaugă o culoare de fundal cu proprietatea `background-color: `,  setându-i valoarea `antiquewhite`.

**Notă**
Fișierul `index.html` conține deja structura de bază. Editează doar partea din editorul CSS.
