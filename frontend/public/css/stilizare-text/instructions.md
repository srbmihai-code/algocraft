# Stilizare text

Textul poate fi editat in CSS cu urmatorele proprietati:

- Proprietatea `color` setează culoarea textului.
- Proprietatea `font-family` stabilește tipul de font.
- Un alt selector este cel de tag, de exemplu `h1 { ... }`, care aplică stilul pentru toate elementele `<h1>`.
- Poți selecta mai multe elemente în CSS cu ajutorul virgulei: de exemplu `h1, p {}`.
- `font-family` poate include mai multe opțiuni separate prin virgulă, browser-ul o folosește pe prima disponibilă.

### 🎯 Sarcină

Schimbă fontul titlului `h1` și a pragrafului `p` în ***Arial** și culoarea paragrafului în `#4b3621` pentru a crește lizibilitatea.
