# Centrarea textului

Putem folosi `text-align: center` pentru a centra textul aflat în interiorul unui element.

## Selectorul ID

În CSS mai există și un selector pentru atributul `id="valoare"`. Doar un singur element poate să aibă un anumit ID. De obicei este mai puțin folosit decât clasele. El se poate selecta în CSS cu selectorul `#valoare`

### 🎯 Sarcină

Centrează tot textul, aplicând proprietatea la tot containerul și adaugă-i o **culoare roșie** paragrafului cu `id="important"` cu proprietatea `color: red`.
